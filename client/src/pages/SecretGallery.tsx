import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Check, Clipboard, Code2, FileCode2, FilePlus2, FolderArchive, Home, ImagePlus, LockKeyhole, LogOut, Pencil, Search, Save, Sparkles, Trash2, X } from "lucide-react";
import { useEffect, useMemo, useRef, useState, type ReactNode } from "react";
import { useLocation } from "wouter";
import { getActiveCamp, getSupabase, getSupabaseConfig, saveSupabaseConfig } from "@/lib/canonical";

type CodeLanguage = "sql" | "java" | "md";
type VaultDoc = { id: string; title: string; language: CodeLanguage; note: string; code: string; updatedAt: string };
type GalleryItem = { id: string; src: string; name: string; createdAt: string };
type ArchiveCard = { id: string; title: string; docIds: string[]; createdAt: string };

const ROOM_PASSWORD = "1234";
const DOCS_KEY = "nightops-secret-vault-v2";
const IMAGES_KEY = "nightops-secret-gallery-v1";
const ARCHIVE_KEY = "nightops-secret-archive-v1";
const languageMeta: Record<CodeLanguage, { label: string; icon: string; tint: string }> = { sql: { label: "SQL", icon: "▣", tint: "text-cyan-300" }, java: { label: "Java", icon: "◈", tint: "text-fuchsia-300" }, md: { label: "Markdown", icon: "#", tint: "text-yellow-300" } };

const readLocal = <T,>(key: string, fallback: T): T => { try { return JSON.parse(localStorage.getItem(key) || JSON.stringify(fallback)) as T; } catch { return fallback; } };

export default function SecretGallery() {
  const [, setLocation] = useLocation();
  const [unlocked, setUnlocked] = useState(() => sessionStorage.getItem("nightops-gallery-unlocked") === "1");
  const [password, setPassword] = useState(""); const [error, setError] = useState("");
  const camp = getActiveCamp(); const connection = getSupabaseConfig(camp);
  const [supabaseUrl, setSupabaseUrl] = useState(connection?.url || ""); const [supabaseKey, setSupabaseKey] = useState(connection?.anonKey || ""); const [orderTable, setOrderTable] = useState(connection?.orderTable || "vw_st_orders_all_v2");
  const [connectionMessage, setConnectionMessage] = useState(""); const [connectionBusy, setConnectionBusy] = useState(false);
  const [docs, setDocs] = useState<VaultDoc[]>(() => readLocal(DOCS_KEY, [])); const [images, setImages] = useState<GalleryItem[]>(() => readLocal(IMAGES_KEY, [])); const [archives, setArchives] = useState<ArchiveCard[]>(() => readLocal(ARCHIVE_KEY, []));
  const [activeId, setActiveId] = useState<string | null>(() => readLocal<VaultDoc[]>(DOCS_KEY, [])[0]?.id ?? null); const [draggedDocId, setDraggedDocId] = useState<string | null>(null); const [query, setQuery] = useState(""); const [view, setView] = useState<"vault" | "images">("vault"); const [copied, setCopied] = useState(false); const [saved, setSaved] = useState(false); const [preview, setPreview] = useState<GalleryItem | null>(null); const inputRef = useRef<HTMLInputElement>(null);
  const activeDoc = docs.find(doc => doc.id === activeId) ?? null; const filteredDocs = useMemo(() => docs.filter(doc => `${doc.title} ${doc.note} ${doc.code}`.toLowerCase().includes(query.toLowerCase())), [docs, query]);
  useEffect(() => { localStorage.setItem(DOCS_KEY, JSON.stringify(docs)); }, [docs]); useEffect(() => { localStorage.setItem(IMAGES_KEY, JSON.stringify(images)); }, [images]); useEffect(() => { localStorage.setItem(ARCHIVE_KEY, JSON.stringify(archives)); }, [archives]);
  const saveConnection = async () => { setConnectionBusy(true); setConnectionMessage(""); try { const url = supabaseUrl.trim().replace(/\/$/, ""); const anonKey = supabaseKey.trim(); const table = orderTable.trim() || "vw_st_orders_all_v2"; if (!/^https:\/\/[a-z0-9-]+\.supabase\.co$/i.test(url)) throw new Error("URL Supabase ไม่ถูกต้อง"); if (!anonKey) throw new Error("กรุณาใส่ Anon/Publishable Key"); if (/service_role|secret/i.test(anonKey)) throw new Error("ห้ามใส่ service-role หรือ secret key ในหน้าเว็บ"); saveSupabaseConfig({ url, anonKey, orderTable: table }, camp); const api = getSupabase(); if (!api) throw new Error("สร้าง Supabase client ไม่สำเร็จ"); const { error: queryError } = await api.from(table).select("*", { count: "exact", head: true }); if (queryError) throw queryError; setConnectionMessage("เชื่อมต่อแล้ว · บันทึกไว้ในเครื่องนี้"); } catch (e) { setConnectionMessage(e instanceof Error ? e.message : String(e)); } finally { setConnectionBusy(false); } };
  const login = () => { if (password === ROOM_PASSWORD) { sessionStorage.setItem("nightops-gallery-unlocked", "1"); setUnlocked(true); setPassword(""); setError(""); } else setError("รหัสไม่ถูกต้อง"); };
  const createDoc = (language: CodeLanguage = "sql") => { const doc: VaultDoc = { id: crypto.randomUUID(), title: `ไฟล์ใหม่ · ${languageMeta[language].label}`, language, note: "เขียนโน้ตอธิบายไฟล์นี้ไว้ตรงนี้", code: language === "sql" ? "-- พิมพ์ SQL ของคุณตรงนี้\n" : language === "java" ? "// Java playground\n" : "# บันทึกโปรเจค\n", updatedAt: new Date().toISOString() }; setDocs(current => [doc, ...current]); setActiveId(doc.id); setView("vault"); };
  const updateActive = (patch: Partial<VaultDoc>) => { if (!activeId) return; setDocs(current => current.map(doc => doc.id === activeId ? { ...doc, ...patch, updatedAt: new Date().toISOString() } : doc)); };
  const deleteDoc = (id: string) => { if (!window.confirm("ลบไฟล์นี้จากเครื่องนี้ใช่ไหม?")) return; setDocs(current => current.filter(doc => doc.id !== id)); setArchives(current => current.map(card => ({ ...card, docIds: card.docIds.filter(docId => docId !== id) }))); if (activeId === id) setActiveId(docs.find(doc => doc.id !== id)?.id ?? null); };
  const copyCode = async () => { if (!activeDoc) return; await navigator.clipboard?.writeText(activeDoc.code); setCopied(true); window.setTimeout(() => setCopied(false), 1400); };
  const addFiles = (files: FileList | null) => { if (!files) return; Array.from(files).filter(file => file.type.startsWith("image/")).forEach(file => { const reader = new FileReader(); reader.onload = () => setImages(current => [{ id: crypto.randomUUID(), src: String(reader.result), name: file.name, createdAt: new Date().toISOString() }, ...current]); reader.readAsDataURL(file); }); };
  const createArchive = () => { const title = window.prompt("ตั้งชื่อโฟลเดอร์เก็บโค้ด", "SQL งานสำคัญ"); if (!title?.trim()) return; const card = { id: crypto.randomUUID(), title: title.trim(), docIds: [], createdAt: new Date().toISOString() }; setArchives(current => [card, ...current]); setView("images"); };
  const dropDoc = (archiveId: string, docId = draggedDocId) => { if (!docId) return; setArchives(current => current.map(card => card.id === archiveId && !card.docIds.includes(docId) ? { ...card, docIds: [...card.docIds, docId] } : card)); setDraggedDocId(null); };
  const dropOnReferenceTab = () => { if (!draggedDocId) return; const target = archives[0] ?? { id: crypto.randomUUID(), title: "ภาพอ้างอิง · CODE ARCHIVE", docIds: [], createdAt: new Date().toISOString() }; setArchives(current => current.some(card => card.id === target.id) ? current.map(card => card.id === target.id && !card.docIds.includes(draggedDocId) ? { ...card, docIds: [...card.docIds, draggedDocId] } : card) : [target, ...current]); setDraggedDocId(null); setView("images"); };
  const docsIn = (card: ArchiveCard) => card.docIds.map(id => docs.find(doc => doc.id === id)).filter(Boolean) as VaultDoc[];

  if (!unlocked) return <div className="flex min-h-[calc(100vh-2rem)] items-center justify-center bg-[#090611] p-4 text-white"><Card className="neon-card w-full max-w-md rounded-3xl border-fuchsia-400/30 bg-[#130b1e]"><CardHeader className="text-center"><LockKeyhole className="mx-auto h-10 w-10 text-cyan-300" /><CardTitle className="mt-2 text-2xl text-white">NEON ALIAS VAULT</CardTitle><p className="text-sm text-fuchsia-100/55">ST SINGTO STORE · PRIVATE ROOM</p></CardHeader><CardContent><Input autoFocus type="password" value={password} onChange={e => setPassword(e.target.value)} onKeyDown={e => { if (e.key === "Enter") login(); }} placeholder="ใส่รหัสห้อง" className="border-cyan-400/25 bg-black/35 text-center tracking-[0.3em] text-white" />{error ? <p className="mt-2 text-center text-xs text-pink-300">{error}</p> : null}<Button onClick={login} className="mt-4 w-full bg-gradient-to-r from-fuchsia-600 to-violet-700">ปลดล็อกห้อง</Button></CardContent></Card></div>;

  return <div className="cyber-grid min-h-[calc(100vh-2rem)] bg-[#090611] p-2 text-white sm:p-4 lg:p-6"><div className="mx-auto max-w-[1500px] space-y-4"><header className="neon-card relative overflow-hidden rounded-3xl border border-fuchsia-400/25 bg-[#130b1e] p-5 sm:p-6"><div className="absolute -right-20 -top-24 h-72 w-72 rounded-full bg-fuchsia-600/20 blur-3xl" /><div className="relative flex flex-wrap items-center justify-between gap-4"><div><div className="flex items-center gap-2 text-[11px] font-bold uppercase tracking-[0.3em] text-cyan-300"><Sparkles className="h-4 w-4" /> ST SINGTO STORE · NEON ALIAS VAULT</div><h1 className="cyber-title mt-2 text-3xl font-bold text-white sm:text-4xl">คำวิบัติ · โค้ด · หลักฐาน</h1><p className="mt-2 max-w-2xl text-sm text-fuchsia-100/60">แก้คำใหม่จากแอดมิน เก็บ SQL และภาพอ้างอิงไว้ในเครื่องนี้</p></div><div className="flex flex-wrap gap-2"><Button onClick={() => setLocation("/orders")} variant="outline" className="border-cyan-400/30 bg-transparent text-cyan-200"><Home className="mr-2 h-4 w-4" />หน้าออเดอร์</Button><Button onClick={() => createDoc("sql")} className="bg-gradient-to-r from-fuchsia-600 to-violet-700"><FilePlus2 className="mr-2 h-4 w-4" />ไฟล์ใหม่</Button><Button variant="outline" onClick={() => { sessionStorage.removeItem("nightops-gallery-unlocked"); setUnlocked(false); }} className="border-fuchsia-400/25 bg-transparent text-fuchsia-100"><LogOut className="mr-2 h-4 w-4" />ล็อกห้อง</Button></div></div></header><Card className="neon-card rounded-3xl border-cyan-400/20 bg-black/20"><CardHeader><CardTitle className="flex items-center gap-2 text-sm text-white"><LockKeyhole className="h-4 w-4 text-cyan-300" />การเชื่อมต่อฐาน ST · ใส่ครั้งเดียว</CardTitle></CardHeader><CardContent className="grid gap-3 md:grid-cols-3"><Input value={supabaseUrl} onChange={e => setSupabaseUrl(e.target.value)} placeholder="https://ชื่อโปรเจกต์.supabase.co" className="border-fuchsia-400/20 bg-black/25 text-white" /><Input type="password" value={supabaseKey} onChange={e => setSupabaseKey(e.target.value)} placeholder="Anon / Publishable Key" className="border-fuchsia-400/20 bg-black/25 text-white" /><Input value={orderTable} onChange={e => setOrderTable(e.target.value)} placeholder="vw_st_orders_all_v2" className="border-fuchsia-400/20 bg-black/25 font-mono text-white" /><div className="flex items-center gap-2 md:col-span-3"><Button onClick={() => void saveConnection()} disabled={connectionBusy} className="bg-gradient-to-r from-fuchsia-600 to-violet-700">{connectionBusy ? "กำลังตรวจ…" : "บันทึกและทดสอบการเชื่อมต่อ"}</Button><span className="text-xs text-white/50">{connectionMessage}</span></div></CardContent></Card><div className="flex flex-wrap items-center justify-between gap-2"><div className="flex flex-wrap items-center gap-2" onDragOver={e => e.preventDefault()} onDrop={e => { e.preventDefault(); dropOnReferenceTab(); }}><Button onClick={() => setView("vault")} variant="outline" className={view === "vault" ? "border-yellow-300/60 bg-yellow-500/15 text-yellow-200" : "border-orange-300/30 bg-transparent text-orange-100/70"}><Code2 className="mr-2 h-4 w-4" />CODE VAULT · {docs.length}</Button><Button onClick={createArchive} variant="outline" className="border-cyan-400/40 bg-cyan-500/10 text-cyan-200"><FolderArchive className="mr-2 h-4 w-4" />+ โฟลเดอร์เก็บโค้ด</Button><Button onClick={() => setView("images")} variant="outline" className={view === "images" ? "border-orange-300/60 bg-orange-500/15 text-orange-200" : "border-orange-300/30 bg-transparent text-orange-100/70"}><ImagePlus className="mr-2 h-4 w-4" />ภาพอ้างอิง · {images.length}</Button><Button onDragOver={e => e.preventDefault()} onDrop={e => { e.preventDefault(); dropOnReferenceTab(); }} variant="outline" className="border-yellow-400/35 bg-yellow-500/10 text-yellow-200"><FolderArchive className="mr-2 h-4 w-4" />เก็บไฟล์{archives.length ? ` · ${archives.length} โฟลเดอร์` : ""}</Button><span className="hidden text-[10px] text-white/35 lg:inline">ลากไฟล์จากรายการซ้ายมาวางที่นี่</span></div><div className="flex items-center gap-2 text-[10px] uppercase tracking-[0.2em] text-yellow-200/45"><span className="h-2 w-2 animate-pulse rounded-full bg-cyan-300" /> LOCAL VAULT · AUTOSAVE</div></div>{view === "images" ? <Card className="neon-card rounded-3xl border-fuchsia-500/20 bg-[#100817]"><CardHeader className="flex flex-row items-center justify-between"><CardTitle className="text-base text-white">ภาพอ้างอิง / CODE ARCHIVE</CardTitle><div className="flex flex-wrap gap-2"><Button onClick={() => inputRef.current?.click()} className="bg-gradient-to-r from-fuchsia-600 to-violet-700"><ImagePlus className="mr-2 h-4 w-4" />เพิ่มรูป</Button><input ref={inputRef} type="file" accept="image/*" multiple className="hidden" onChange={e => { addFiles(e.target.files); e.currentTarget.value = ""; }} /></div></CardHeader><CardContent>{archives.length ? <div className="mb-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{archives.map(card => <div key={card.id} onDragOver={e => e.preventDefault()} onDrop={e => { e.preventDefault(); dropDoc(card.id); }} className="neon-card min-h-36 rounded-2xl border border-cyan-400/25 bg-black/25 p-4"><div className="flex items-start justify-between gap-2"><div><p className="text-[10px] uppercase tracking-[0.2em] text-cyan-300">CODE FOLDER</p><h3 className="mt-1 font-semibold text-white">{card.title}</h3></div><button type="button" onClick={() => setArchives(current => current.filter(x => x.id !== card.id))} className="text-pink-300/70 hover:text-pink-200"><Trash2 className="h-4 w-4" /></button></div><div className="mt-3 space-y-1">{docsIn(card).length ? docsIn(card).map(doc => <button key={doc.id} type="button" onClick={() => { setActiveId(doc.id); setView("vault"); }} className="block w-full truncate rounded-lg bg-white/[0.04] px-2 py-1 text-left font-mono text-[11px] text-cyan-200">{languageMeta[doc.language].icon} {doc.title}</button>) : <p className="text-[11px] text-white/30">วางไฟล์ตรงนี้เพื่อเก็บเข้ากล่อง</p>}</div></div>)}</div> : null}{images.length ? <div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-5">{images.map(item => <div key={item.id} className="group overflow-hidden rounded-2xl border border-fuchsia-500/20 bg-black/20"><button type="button" onClick={() => setPreview(item)} className="block aspect-[4/3] w-full overflow-hidden"><img src={item.src} alt={item.name} className="h-full w-full object-cover transition duration-200 group-hover:scale-105" /></button><div className="flex items-center justify-between gap-2 p-2"><p className="truncate text-[10px] text-fuchsia-100/65">{item.name}</p><button type="button" onClick={() => setImages(current => current.filter(x => x.id !== item.id))} className="text-pink-300/70"><Trash2 className="h-3.5 w-3.5" /></button></div></div>)}</div> : <EmptyState icon={<ImagePlus />} text="ยังไม่มีภาพอ้างอิง" />}</CardContent></Card> : <div className="grid min-h-[620px] gap-4 lg:grid-cols-[260px_minmax(0,1fr)_300px]"><Card className="neon-card overflow-hidden rounded-3xl border-fuchsia-500/20 bg-[#100817]"><CardHeader className="border-b border-white/5 p-4"><div className="flex items-center justify-between"><CardTitle className="text-sm text-white">FILES / NOTES</CardTitle><button type="button" onClick={() => createDoc("sql")} className="rounded-lg p-2 text-cyan-300 hover:bg-cyan-500/10"><FilePlus2 className="h-4 w-4" /></button></div><div className="relative mt-3"><Search className="absolute left-3 top-2.5 h-3.5 w-3.5 text-white/30" /><Input value={query} onChange={e => setQuery(e.target.value)} placeholder="ค้นหาไฟล์..." className="h-9 border-white/10 bg-black/25 pl-9 text-xs text-white" /></div></CardHeader><CardContent className="max-h-[540px] space-y-2 overflow-auto p-3">{filteredDocs.length ? filteredDocs.map(doc => <button key={doc.id} type="button" draggable onDragStart={() => setDraggedDocId(doc.id)} onDragEnd={() => setDraggedDocId(null)} onClick={() => setActiveId(doc.id)} className={`w-full rounded-2xl border p-3 text-left transition ${activeId === doc.id ? "border-cyan-400/50 bg-cyan-500/10" : "border-white/5 bg-black/15 hover:border-fuchsia-400/30"}`}><div className="flex items-center justify-between gap-2"><span className={`font-mono text-[10px] font-bold ${languageMeta[doc.language].tint}`}>{languageMeta[doc.language].icon} {languageMeta[doc.language].label}</span><span className="text-[9px] text-white/25">{new Date(doc.updatedAt).toLocaleDateString("th-TH")}</span></div><p className="mt-2 truncate text-sm font-semibold text-white/85">{doc.title}</p><p className="mt-1 truncate text-[10px] text-white/35">{doc.note}</p></button>) : <EmptyState icon={<FileCode2 />} text="ยังไม่มีไฟล์ · กด + เพื่อสร้าง" />}</CardContent></Card><Card className="neon-card overflow-hidden rounded-3xl border-fuchsia-500/20 bg-[#0e0715]"><CardHeader className="border-b border-white/5 p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex min-w-0 items-center gap-2"><Pencil className="h-4 w-4 shrink-0 text-cyan-300" /><Input value={activeDoc?.title ?? "ยังไม่ได้เลือกไฟล์"} onChange={e => updateActive({ title: e.target.value })} className="h-9 min-w-0 border-white/10 bg-black/20 text-sm font-semibold text-white" disabled={!activeDoc} /></div><div className="flex gap-2"><Button onClick={() => { setSaved(true); window.setTimeout(() => setSaved(false), 1400); }} disabled={!activeDoc} className="h-9 bg-gradient-to-r from-fuchsia-600 to-violet-700">{saved ? <Check className="mr-2 h-4 w-4" /> : <Save className="mr-2 h-4 w-4" />}{saved ? "บันทึกแล้ว" : "บันทึก"}</Button><Button onClick={copyCode} disabled={!activeDoc} variant="outline" className="h-9 border-white/10 bg-transparent text-white">{copied ? <Check className="mr-2 h-4 w-4 text-emerald-300" /> : <Clipboard className="mr-2 h-4 w-4" />}{copied ? "คัดลอกแล้ว" : "คัดลอก"}</Button></div></div><div className="mt-3 flex flex-wrap items-center gap-2"><select value={activeDoc?.language ?? "sql"} onChange={e => updateActive({ language: e.target.value as CodeLanguage })} disabled={!activeDoc} className="h-8 rounded-lg border border-fuchsia-400/25 bg-black/35 px-3 font-mono text-xs text-fuchsia-200 outline-none"><option value="sql">SQL</option><option value="java">Java</option><option value="md">Markdown</option></select><span className="text-[10px] uppercase tracking-[0.18em] text-white/30">ลากรายการซ้ายไปเก็บที่ภาพอ้างอิง · autosave local</span></div></CardHeader>{activeDoc ? <CardContent className="space-y-3 p-4"><textarea value={activeDoc.note} onChange={e => updateActive({ note: e.target.value })} placeholder="โน้ตอธิบายไฟล์นี้..." className="min-h-16 w-full resize-y rounded-xl border border-fuchsia-400/15 bg-fuchsia-500/[0.04] p-3 text-sm text-fuchsia-100/75 outline-none focus:border-cyan-400/50" /><div className="code-shell overflow-hidden rounded-2xl border border-cyan-400/25 bg-[#08050e]"><div className="flex items-center justify-between border-b border-white/5 bg-white/[0.03] px-4 py-2"><span className="font-mono text-[10px] text-cyan-300">{languageMeta[activeDoc.language].icon} {languageMeta[activeDoc.language].label} · LIVE EDITOR</span><span className="text-[10px] text-white/25">{activeDoc.code.split("\n").length} lines · ลากเลื่อนได้</span></div><textarea spellCheck={false} value={activeDoc.code} onChange={e => updateActive({ code: e.target.value })} className="h-[62vh] max-h-[680px] min-h-[360px] w-full resize-none overflow-auto whitespace-pre bg-transparent p-4 font-mono text-[12px] leading-6 text-cyan-50/90 outline-none selection:bg-fuchsia-400/30" /></div></CardContent> : <EmptyState icon={<Code2 />} text="เลือกไฟล์ทางซ้าย หรือสร้างไฟล์ใหม่" />}</Card><Card className="neon-card rounded-3xl border-fuchsia-500/20 bg-[#100817]"><CardHeader><CardTitle className="flex items-center gap-2 text-sm text-white"><FileCode2 className="h-4 w-4 text-cyan-300" />VAULT CONTROL</CardTitle></CardHeader><CardContent className="space-y-4 text-xs text-white/55"><div className="rounded-2xl border border-cyan-400/15 bg-cyan-500/[0.04] p-4"><p className="text-[10px] uppercase tracking-[0.2em] text-cyan-300">ACTIVE FILE</p><p className="mt-2 truncate text-base font-semibold text-white">{activeDoc?.title ?? "—"}</p><p className="mt-1 text-white/35">{activeDoc ? `${languageMeta[activeDoc.language].label} · ${activeDoc.code.length.toLocaleString()} chars` : "ยังไม่ได้เลือก"}</p></div><div><p className="mb-2 text-[10px] uppercase tracking-[0.2em] text-white/30">QUICK CREATE</p><div className="grid grid-cols-3 gap-2">{(["sql", "java", "md"] as CodeLanguage[]).map(lang => <button key={lang} type="button" onClick={() => createDoc(lang)} className="rounded-xl border border-white/10 bg-black/20 p-3 font-mono text-xs text-cyan-200 hover:border-fuchsia-400/40 hover:bg-fuchsia-500/10">{languageMeta[lang].icon} {languageMeta[lang].label}</button>)}</div></div><div className="rounded-2xl border border-pink-400/20 bg-pink-500/[0.04] p-4"><p className="text-[10px] uppercase tracking-[0.2em] text-pink-300">DANGER ZONE</p><Button onClick={() => activeId && deleteDoc(activeId)} disabled={!activeDoc} variant="outline" className="mt-3 w-full border-pink-400/25 bg-transparent text-pink-200"><Trash2 className="mr-2 h-4 w-4" />ลบไฟล์นี้</Button></div></CardContent></Card></div>}</div>{preview ? <div className="fixed inset-0 z-[80] flex items-center justify-center bg-black/85 p-4" onClick={() => setPreview(null)}><button type="button" className="absolute right-5 top-5 rounded-full bg-white/10 p-2 text-white" onClick={() => setPreview(null)}><X className="h-5 w-5" /></button><img src={preview.src} alt={preview.name} className="max-h-[90vh] max-w-[95vw] rounded-2xl object-contain" /></div> : null}</div>;
}
function EmptyState({ icon, text }: { icon: ReactNode; text: string }) { return <div className="flex min-h-32 flex-col items-center justify-center rounded-2xl border border-dashed border-cyan-500/20 p-6 text-center text-xs text-white/35">{icon}<span className="mt-2">{text}</span></div>; }
